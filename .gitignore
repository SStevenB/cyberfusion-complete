venv/
__pycache__/
*.pyc
*.pyo
.DS_Store
data/raw/
data/processed/
data/outputs/
data/history/
config/config.yaml
.pytest_cache/
*.egg-info/
.env

# user-uploaded evidence (privacy — keep local)
data/uploads/*
!data/uploads/.gitkeep

# configured platform state (local to each user — never commit)
data/secrets.local.json
data/workspace.json

# ── React frontend (Option B) ─────────────────────────────────────────────────
frontend/node_modules/
frontend/dist/
frontend/.vite/
# generated by assemble.mjs from src/mockup/* — rebuilt on every build
frontend/src/CyberFusionApp.jsx
data/finding_status.json
